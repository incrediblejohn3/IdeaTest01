server:
  port: 80
  tomcat:
    uri-encoding: utf-8
    max-threads: 1000
    min-spare-threads: 100
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mysql?serverTimezone=GMT%2B8&characterEncoding=UTF8
    username: root
    password: root
  thymeleaf:
    prefix: classpath:/templates/

